[
  {
    "what": "Paired theme-color meta tags, so the iOS status bar area matches the grouped background in each mode. status-bar-style stays default and the viewport line at line 5 is not touched.",
    "where": "head, line 10",
    "before": "<meta name=\"theme-color\" content=\"#0284c7\">",
    "after": "<meta name=\"theme-color\" content=\"#f2f2f7\" media=\"(prefers-color-scheme: light)\">\n<meta name=\"theme-color\" content=\"#000000\" media=\"(prefers-color-scheme: dark)\">",
    "risk": "None. Outside the script. An unsupported media attribute on theme-color degrades to no theme colour, not a wrong one."
  },
  {
    "what": "Replace the entire contents of the inline <style> block with the stylesheet in the stylesheet field. Delete everything from line 16 (the opening of :root) through line 129 (the closing brace of @media print) and paste the new sheet between the untouched <style> and </style> tags. The before snippet below is the unique anchor at the top of the range.",
    "where": "index.html lines 15 to 130",
    "before": "  :root{\n    --bg:#f3f6f8; --card:#ffffff; --ink:#13212b; --muted:#5f7280; --line:#e1e8ed;\n    --accent:#0284c7; --accent-ink:#075985; --accent-bg:#e0f2fe;\n    --good:#15803d; --good-bg:#dcfce7; --bad:#dc2626; --bad-bg:#fef2f2;\n    --warn:#b45309; --warn-bg:#fff7ed;\n  }",
    "after": "The full text of the stylesheet field, verbatim, starting with the file header comment block and ending with the prefers-reduced-motion block. 849 lines. Section 15 of it keeps header, nav.tabs, .hrow, .lrow, .filters and the old pool class names alive so the app still looks right while the later edits land one at a time.",
    "risk": "Zero harness risk: verify.js reads index.html only for the script extraction regex and never parses CSS. Confirmed by running the suite with the new sheet in place. The app is dark capable and contrast correct at the end of this edit, still on the old markup."
  },
  {
    "what": "Delete the dead html: branch in h(). It has no call sites and it is the only route to innerHTML in the file, so it should not be left lying around for someone to find.",
    "where": "h(), line 157",
    "before": "      else if (k === \"html\") e.innerHTML = v;\n",
    "after": "",
    "risk": "None. Zero call sites, confirmed by grep. Suite passes."
  },
  {
    "what": "Replace the old header and wrap with the static shell: glass top bar, the scrolling large title, the glass tab bar with hand written inline SVG icons, the scrim and the sheet frame. Bar buttons start with class \"barbtn gone\" rather than the hidden attribute, because the script can rewrite a class but cannot remove an attribute. #who stays in the markup, hidden, so renderWho keeps working until edit 20 removes it.",
    "where": "body, lines 133 to 140",
    "before": "<div class=\"wrap\">\n  <header>\n    <h1>🏊 W&amp;S Trainings</h1>\n    <span class=\"spacer\"></span>\n    <span id=\"who\"></span>\n  </header>\n  <div id=\"app\"></div>\n</div>",
    "after": "<div class=\"topbar glass\" id=\"topbar\">\n  <button class=\"barbtn gone\" id=\"bar-left\"></button>\n  <div class=\"topbar-title\" id=\"topbar-title\"></div>\n  <span id=\"who\" hidden></span>\n  <button class=\"barbtn gone\" id=\"bar-right\"></button>\n</div>\n\n<div class=\"wrap\">\n  <h1 class=\"largetitle\" id=\"largetitle\"></h1>\n  <div id=\"app\"></div>\n</div>\n\n<nav class=\"tabbar glass\" id=\"tabbar\" aria-label=\"Views\">\n  <button class=\"tab\" id=\"tab-today\" aria-label=\"Today\">\n    <svg viewBox=\"0 0 28 28\" aria-hidden=\"true\"><rect x=\"3.5\" y=\"5.5\" width=\"21\" height=\"19\" rx=\"4\"/><path d=\"M3.5 11.5h21M9 3.5v4M19 3.5v4\"/></svg>\n    <span class=\"lbl\">Today</span>\n  </button>\n  <button class=\"tab\" id=\"tab-history\" aria-label=\"History\">\n    <svg viewBox=\"0 0 28 28\" aria-hidden=\"true\"><path d=\"M14 8.5V14l4 2.5\"/><path d=\"M24.5 14a10.5 10.5 0 1 1-3.1-7.4\"/><path d=\"M24.5 3.5V7H21\"/></svg>\n    <span class=\"lbl\">History</span>\n  </button>\n  <button class=\"tab\" id=\"tab-library\" aria-label=\"Library\">\n    <svg viewBox=\"0 0 28 28\" aria-hidden=\"true\"><rect x=\"4\" y=\"9.5\" width=\"20\" height=\"15\" rx=\"3\"/><path d=\"M6.5 6.5h15M9 3.5h10\"/></svg>\n    <span class=\"lbl\">Library</span>\n  </button>\n  <button class=\"tab\" id=\"tab-insights\" aria-label=\"Insights\">\n    <svg viewBox=\"0 0 28 28\" aria-hidden=\"true\"><path d=\"M4 24h20\"/><path d=\"M8.5 24v-6.5M14 24V8.5M19.5 24v-11\"/></svg>\n    <span class=\"lbl\">Insights</span>\n  </button>\n</nav>\n\n<div class=\"scrim\" id=\"scrim\"></div>\n<div class=\"sheet\" id=\"sheet\" role=\"dialog\" aria-modal=\"true\" aria-labelledby=\"sheet-head\">\n  <div class=\"grabber\"></div>\n  <div class=\"sheet-head\" id=\"sheet-head\"></div>\n  <div class=\"sheet-body\" id=\"sheet-body\"></div>\n</div>",
    "risk": "This edit and the next one must land together. Element ids are invisible to the harness: doc.getElementById returns a fresh stub object for every call and never null, so a typo passes the suite and dies in the browser. Hand check all eleven ids against edit 5 and open the app in a real browser."
  },
  {
    "what": "Shell element references, the paint helpers, the sheet helpers, the grouped list helpers and the one time event wiring. Everything here touches the shell only through className, textContent, setAttribute, append and onclick. Bar buttons use onclick assignment rather than addEventListener because they are static and survive every render, so addEventListener would stack a new handler each time.",
    "where": "after var WHO, lines 173 to 174",
    "before": "  var APP = document.getElementById(\"app\");\n  var WHO = document.getElementById(\"who\");\n",
    "after": "  var APP = document.getElementById(\"app\");\n  var WHO = document.getElementById(\"who\");\n\n  // The shell lives in static HTML because the icons have to be real <svg>\n  // elements and h() cannot build those. This script only ever touches these\n  // through className, textContent, setAttribute, append and onclick.\n  var TOPBAR   = document.getElementById(\"topbar\");\n  var BARTITLE = document.getElementById(\"topbar-title\");\n  var BARL     = document.getElementById(\"bar-left\");\n  var BARR     = document.getElementById(\"bar-right\");\n  var BIGTITLE = document.getElementById(\"largetitle\");\n  var TABBAR   = document.getElementById(\"tabbar\");\n  var SCRIM    = document.getElementById(\"scrim\");\n  var SHEET    = document.getElementById(\"sheet\");\n  var SHEETHD  = document.getElementById(\"sheet-head\");\n  var SHEETBD  = document.getElementById(\"sheet-body\");\n  var TABS = [\"today\",\"history\",\"library\",\"insights\"];\n  var TABEL = {};\n  TABS.forEach(function(n){ TABEL[n] = document.getElementById(\"tab-\" + n); });\n\n  // Every class string is rebuilt whole. There is no classList in this app, so\n  // nothing appends to a class name anywhere.\n  var bodyFlags = { kbd:false, solid:false };\n  function paintBody(){\n    var c = [];\n    if (bodyFlags.kbd) c.push(\"kbd\");\n    if (bodyFlags.solid) c.push(\"solid\");\n    document.body.className = c.join(\" \");\n  }\n  var barCompact = false;\n  function paintTopbar(){ TOPBAR.className = \"topbar glass\" + (barCompact ? \" compact\" : \"\"); }\n  function onScroll(){\n    var y = window.pageYOffset || 0;\n    var want = y > 28;\n    if (want === barCompact) return;\n    barCompact = want; paintTopbar();\n  }\n  // Static elements, so the handler is set, not added. addEventListener on every\n  // render would stack up a new handler each time.\n  function setBar(el, label, fn){\n    clear(el);\n    el.className = \"barbtn\" + (label ? \"\" : \" gone\");\n    el.onclick = fn || null;\n    if (label) el.append(document.createTextNode(label));\n  }\n  function clearBars(){\n    BIGTITLE.textContent = \"\";\n    BARTITLE.textContent = \"\";\n    setBar(BARL, null, null);\n    setBar(BARR, null, null);\n  }\n  function showTabs(on){ TABBAR.className = \"tabbar glass\" + (on ? \"\" : \" gone\"); }\n  function paintTabs(){\n    TABS.forEach(function(n){\n      var el = TABEL[n];\n      el.className = \"tab\" + (view.name === n ? \" on\" : \"\");\n      el.setAttribute(\"aria-current\", view.name === n ? \"page\" : \"false\");\n    });\n  }\n  function setTitle(big, compact){\n    BIGTITLE.textContent = big || \"\";\n    BARTITLE.textContent = compact || big || \"\";\n  }\n\n  // Sheets: everything that used to be a select, an alert or a confirm. Detents\n  // are class names, never measured heights.\n  var sheetLive = false;\n  function openSheet(title, size, body){\n    var pool = view.name === \"pool\";\n    clear(SHEETHD); clear(SHEETBD);\n    if (title) SHEETHD.append(document.createTextNode(title));\n    (Array.isArray(body) ? body : [body]).forEach(function(n){ if (n) SHEETBD.append(n); });\n    SHEET.className = \"sheet \" + (size || \"med\") + (pool ? \" pool\" : \"\") + \" on\";\n    SCRIM.className = \"scrim\" + (pool ? \" pool\" : \"\") + \" on\";\n    sheetLive = true;\n  }\n  function closeSheet(){\n    SHEET.className = \"sheet\";\n    SCRIM.className = \"scrim\";\n    sheetLive = false;\n    setTimeout(function(){ if (!sheetLive){ clear(SHEETBD); clear(SHEETHD); } }, 420);\n  }\n\n  // Grouped inset list. A tappable row is a real <button>, because a div with an\n  // onclick is invisible to VoiceOver, cannot be reached by keyboard and gets\n  // none of the free hit slop. A button may only hold inline content, so the\n  // parts are spans and the stylesheet makes them stack.\n  function irow(o){\n    var kids = [];\n    if (o.date) kids.push(h(\"span\",{class:\"r-date\"}, o.date));\n    kids.push(h(\"span\",{class:\"r-main\"},\n      h(\"span\",{class:\"r-1\"}, o.title),\n      o.sub ? h(\"span\",{class:\"r-2\"}, o.sub) : null,\n      o.foot ? h(\"span\",{class:\"r-3\"}, o.foot) : null));\n    (o.badges || []).forEach(function(b){ kids.push(b); });\n    if (o.value != null) kids.push(h(\"span\",{class:\"r-val\"}, o.value));\n    if (o.onclick) kids.push(h(\"span\",{class:\"chev\",\"aria-hidden\":\"true\"},\"›\"));\n    var cls = \"irow\" + (o.onclick ? \" tap\" : \"\") + (o.tone ? \" \" + o.tone : \"\") + (o.act ? \" act\" : \"\");\n    if (!o.onclick) return h(\"div\",{class:cls}, kids);\n    return h(\"button\",{ class:cls, onclick:o.onclick, disabled:o.disabled }, kids);\n  }\n  function listCard(header, rows){\n    var live = (rows || []).filter(Boolean);\n    if (!live.length) return null;\n    return h(\"div\",{class:\"list\"},\n      header ? h(\"div\",{class:\"list-hdr\"}, header) : null,\n      h(\"div\",{class:\"list-card\"}, live));\n  }\n  function sheetList(rows){ return h(\"div\",{class:\"list-card\"}, (rows||[]).filter(Boolean)); }\n  function stat(val, lbl){ return h(\"div\",null, h(\"div\",{class:\"val\"},val), h(\"div\",{class:\"lbl\"},lbl)); }\n\n  TABS.forEach(function(n){ TABEL[n].addEventListener(\"click\", function(){ nav(n); }); });\n  SCRIM.addEventListener(\"click\", closeSheet);\n  window.addEventListener(\"scroll\", onScroll, { passive:true });\n  document.addEventListener(\"focusin\", function(e){\n    var n = e.target && e.target.tagName;\n    if (n === \"INPUT\" || n === \"TEXTAREA\" || n === \"SELECT\"){ bodyFlags.kbd = true; paintBody(); }\n  });\n  document.addEventListener(\"focusout\", function(){ bodyFlags.kbd = false; paintBody(); });\n",
    "risk": "All of this is top level, which is the policed surface. Tested: the suite passes. window.pageYOffset is only read inside the scroll handler, which never fires under verify. window.addEventListener, document.addEventListener and el.addEventListener all exist in the stub as no-ops. el.onclick is a plain property assignment, which cannot throw on a stub object."
  },
  {
    "what": "State the new screens need: the session detail route, the Today setup values hoisted out of renderToday so Regenerate can reach them, and the persistent History search term. The search term is why a repaint no longer clears what he typed.",
    "where": "state block, lines 247 and 252",
    "before": "  var view = { name:\"today\" };",
    "after": "  var view = { name:\"today\", from:\"history\", session:null };\n  // Lifted out of renderToday so a repaint keeps the choices and so Regenerate\n  // can build the same options object.\n  var setup = { date:null, slot:null, ending:null, focusKey:\"\" };\n  function todayOpts(replace){\n    return { date:setup.date, slot:setup.slot, group:\"advanced\",\n             ending:setup.ending, focusKey:setup.focusKey || null, replace:!!replace };\n  }",
    "risk": "Top level, plain objects only. Add `var hist = { q:\"\" };` on the line after `var lib = ...` at line 252 in the same edit."
  },
  {
    "what": "render() drives the shell instead of building a nav. The nav.tabs build is deleted, the offline banner becomes an edge to edge warn strip, the two dead ends get a retry, and a new session route renders a saved session as its own screen with a back button so it never overwrites Today.",
    "where": "render(), lines 1441 to 1480",
    "before": "  function render(){\n    renderWho();\n    var pool = document.getElementById(\"pool\");\n    if (pool) pool.remove();\n    clear(APP);\n\n    if (!configured){\n      APP.append(h(\"div\",{class:\"card stack\"},\n        h(\"h2\",null,\"Almost there, needs setup\"),\n        h(\"p\",{class:\"muted\"},\"This app is not connected to its database yet. Open \",\n          h(\"b\",null,\"config.js\"), \" and fill in your Supabase project URL and anon key (see \",\n          h(\"b\",null,\"SETUP.md\"), \").\")\n      ));\n      return;\n    }\n    if (!window.supabase){\n      APP.append(h(\"div\",{class:\"card\"},\n        h(\"p\",null,\"Couldn't load the database library. Check your internet connection and reload.\")));\n      return;\n    }\n    if (!session) return renderLogin();\n    if (notCoach) return renderNotCoach();\n\n    if (view.name === \"pool\" && draft) return renderPool();\n\n    if (offline) APP.append(h(\"div\",{class:\"banner\"},\n      staleMirror ? \"No connection, and the copy on this phone is more than ten days old. Generate will still work but check the sets.\"\n                  : \"No connection. Working from the copy saved on this phone. Nothing will be saved until you are back online.\"));\n\n    APP.append(h(\"nav\",{class:\"tabs\"},\n      [[\"today\",\"Today\"],[\"history\",\"History\"],[\"library\",\"Library\"],[\"insights\",\"Insights\"]].map(function(p){\n        return h(\"button\",{ class: view.name===p[0] ? \"active\" : \"\", onclick:function(){ nav(p[0]); } }, p[1]);\n      })\n    ));\n\n    if (view.name === \"history\")  return renderHistory();\n    if (view.name === \"library\")  return renderLibrary();\n    if (view.name === \"insights\") return renderInsights();\n    return renderToday();\n  }",
    "after": "  function render(){\n    renderWho();\n    var pool = document.getElementById(\"pool\");\n    if (pool) pool.remove();\n    clear(APP);\n    clearBars();\n    barCompact = false; paintTopbar();\n\n    if (!configured){\n      showTabs(false); setTitle(\"Setup\", \"Setup\");\n      APP.append(h(\"div\",{class:\"card stack\"},\n        h(\"h2\",null,\"Almost there, needs setup\"),\n        h(\"p\",{class:\"muted\"},\"This app is not connected to its database yet. Open \",\n          h(\"b\",null,\"config.js\"), \" and fill in your Supabase project URL and anon key (see \",\n          h(\"b\",null,\"SETUP.md\"), \").\")\n      ));\n      return;\n    }\n    if (!window.supabase){\n      showTabs(false); setTitle(\"No connection\", \"No connection\");\n      APP.append(h(\"div\",{class:\"card stack\"},\n        h(\"p\",null,\"Couldn't load the database library. Check your internet connection and reload.\"),\n        h(\"button\",{class:\"primary big\",onclick:function(){ window.location.reload(); }},\"Try again\")));\n      return;\n    }\n    if (!session){ showTabs(false); return renderLogin(); }\n    if (notCoach){ showTabs(false); return renderNotCoach(); }\n\n    if (view.name === \"pool\" && draft){ showTabs(false); return renderPool(); }\n\n    showTabs(true);\n    paintTabs();\n\n    if (offline) APP.append(h(\"div\",{class:\"banner warn strip\"},\n      staleMirror ? \"No connection, and the copy on this phone is more than ten days old. Generate will still work but check the sets.\"\n                  : \"No connection. Working from the copy saved on this phone. Nothing will be saved until you are back online.\"));\n\n    if (view.name === \"session\")  return renderSessionScreen();\n    if (view.name === \"history\")  return renderHistory();\n    if (view.name === \"library\")  return renderLibrary();\n    if (view.name === \"insights\") return renderInsights();\n    return renderToday();\n  }\n\n  // A saved session opened from History is its own screen with its own way back.\n  // It used to be dropped into draft, which silently rewrote the date picker and\n  // relabelled the generate button.\n  function renderSessionScreen(){\n    var sk = view.session;\n    if (!sk){ view.name = \"history\"; return renderHistory(); }\n    setTitle(fmtDayDate(sk.date), sk.title);\n    setBar(BARL, \"History\", function(){\n      view.name = view.from || \"history\"; view.session = null; render(); });\n    renderSessionCard(sk, false);\n  }",
    "risk": "The not-configured branch is reached synchronously during vm.runInContext, because boot() calls render() before its first await. Tested: the suite passes and the probe calls render() directly. window.location.reload is only inside a click handler, which verify never fires."
  },
  {
    "what": "toast() picks its register from the view and stops drawing under pool view. It was z-index 99 against #pool at 50, so the light green toast covered the deck. The inline styles become the .toast class.",
    "where": "toast(), lines 1404 to 1411",
    "before": "  var toastEl = null;\n  function toast(msg){\n    if (toastEl) toastEl.remove();\n    toastEl = h(\"div\",{ class:\"banner ok\", style:{position:\"fixed\",left:\"50%\",transform:\"translateX(-50%)\",\n      bottom:\"24px\",zIndex:\"99\",maxWidth:\"90%\",boxShadow:\"0 2px 10px rgba(0,0,0,.15)\"} }, msg);\n    document.body.append(toastEl);\n    setTimeout(function(){ if (toastEl){ toastEl.remove(); toastEl = null; } }, 2600);\n  }",
    "after": "  var toastEl = null;\n  function toast(msg){\n    if (toastEl) toastEl.remove();\n    // On the deck the toast follows pool rules and has to sit above pool view,\n    // which the old z-index of 99 did not.\n    toastEl = h(\"div\",{ class: view.name === \"pool\" ? \"toast pool\" : \"toast\" }, msg);\n    document.body.append(toastEl);\n    setTimeout(function(){ if (toastEl){ toastEl.remove(); toastEl = null; } }, 2600);\n  }",
    "risk": "None. Tested, suite passes."
  },
  {
    "what": "Retired sets stop showing as active. Retire writes active:false and never touches status, but the row filter only read status, so a retired set kept appearing, looked identical to a live one and still offered Retire. One shared predicate now decides it everywhere.",
    "where": "renderLibrary(), lines 1840 to 1846",
    "before": "      var rows = data.library.filter(function(x){\n        if ((x.status||\"active\") !== \"active\") return false;",
    "after": "      var rows = data.library.filter(function(x){\n        if (!isActive(x)) return false;",
    "risk": "Fewer rows will show, which is the point. Add the predicate itself in the same edit, at the top level near thinRoles: `// Retired sets used to keep showing as if they were active, because the row\\n  // filter only looked at status while Retire only writes active.\\n  function isActive(x){ return (x.status||\"active\") === \"active\" && x.active !== false; }`. Unasserted by verify, so the smoke test from harness commit 1 is the only net."
  },
  {
    "what": "The three counts he uses to judge library health were counting by status only, so all three overstated the active pool. roleCounts and thinRoles already excluded active:false, which is what made the inconsistency provable.",
    "where": "lines 1881, 1986 and 2020",
    "before": "        h(\"span\",{class:\"badge\"}, data.library.filter(function(x){ return (x.status||\"active\")===\"active\"; }).length + \" sets\"),",
    "after": "        h(\"span\",{class:\"badge\"}, data.library.filter(isActive).length + \" sets\"),",
    "risk": "Apply the same substitution at line 1986 (the Insights Library tile) and line 2020 (the never used denominator). A grouped list showing a wrong count is worse than an ugly list showing a wrong count, which is why this lands before the restyling of those screens."
  },
  {
    "what": "hydrate carries the warm-up row's id and done flag. The warm-up is a real tr_session_items row with a done column, and hydrate already finds it, but threw everything away except the minutes. Pool view cannot tick it without these two fields.",
    "where": "hydrate(), line 1333",
    "before": "      warmup_minutes: warm ? (data.settings.warmup_minutes||10) : (data.settings.warmup_minutes||10),",
    "after": "      warmup_minutes: (data.settings && data.settings.warmup_minutes) || 10,\n      // The warm-up is a real saved row, so its tick has somewhere to go.\n      warmItemId: warm ? warm.id : null,\n      warmDone: warm ? !!warm.done : false,",
    "risk": "hydrate is not exported and not asserted. The old line had identical branches, so collapsing it changes nothing. It also stops throwing when data.settings is null."
  },
  {
    "what": "Replace the whole pool view section. New: one open block and the rest collapsed to a line, the call and rest and qualifier split, spelled rest units in two rows for Adv 1 and Adv 2, a 104px tick strip instead of a whole card tap, the signed drift, a Close confirmation that no longer destroys the clock, and the tick gate. Delete startPool, renderPool and togglePool as they stand and paste this in their place.",
    "where": "lines 1691 to 1764, from the Pool view banner comment down to the end of togglePool, up to but not including the History banner comment",
    "before": "  // ------------------------------------------------------------- Pool view ---\n  function startPool(sk){\n    draft = sk; view.name = \"pool\";\n    poolState = { startedAt: Date.now() };\n    render();\n  }",
    "after": "  // ------------------------------------------------------------- Pool view ---\n  // The hard contrast register. Nothing below reads a theme token, nothing\n  // animates, and the only tappable thing inside a block is the tick strip.\n  var POOL_UI = { big:false, night:false, sound:true };\n  var poolEl = null, poolTickAt = 0, poolScrollAt = 0, poolUndo = null;\n\n  // There is no haptics in Safari on iOS, so a tick cannot be confirmed by\n  // feel. A short click is built on the tap itself, which is what keeps it\n  // inside the gesture iOS wants, and it needs no file.\n  var AC = (typeof AudioContext === \"function\") ? AudioContext\n         : (typeof webkitAudioContext === \"function\") ? webkitAudioContext : null;\n  var poolAudio = null;\n  function poolClick(){\n    if (!POOL_UI.sound || !AC) return;\n    try{\n      if (!poolAudio) poolAudio = new AC();\n      var o = poolAudio.createOscillator(), g = poolAudio.createGain();\n      o.type = \"square\"; o.frequency.value = 1500; g.gain.value = 0.09;\n      o.connect(g); g.connect(poolAudio.destination);\n      o.start(); o.stop(poolAudio.currentTime + 0.04);\n    }catch(e){}\n  }\n\n  // The call is what he shouts across the water: reps by distance, nothing\n  // else. Checked against the real library: 418 of 419 set lines split, the\n  // longest call is six characters, so at 48px it is about 211px against 311px\n  // of usable width on the narrowest iPhone. It can never wrap. The one line\n  // that does not split has no reps in it, so the whole line becomes detail.\n  var CALL_RE = /^\\s*(\\d+\\s*[x×]\\s*\\d+(?:\\s*m)?|\\d+\\s*m)\\s*(.*)$/i;\n  function poolLines(t){\n    var s = String(t == null ? \"\" : t).replace(/\\s+/g,\" \").trim();\n    var m = CALL_RE.exec(s);\n    if (!m) return { call:\"\", qual:s };\n    return { call:m[1].replace(/\\s+/g,\" \").trim(),\n             qual:m[2].replace(/^[\\s,;:.\\-–—]+/,\"\") };\n  }\n  // Widths are estimated from a table, not measured, because there is no way to\n  // measure an element from this script.\n  var EM_W = { \" \":.26, i:.28, l:.28, j:.3, f:.3, t:.34, r:.35, s:.45, c:.5, e:.52,\n               a:.52, z:.45, x:.52, v:.52, y:.52, n:.56, o:.56, u:.56, b:.55, d:.55,\n               p:.55, q:.55, g:.55, h:.55, k:.52, m:.86, w:.78, I:.28, M:.9 };\n  function emWidth(s){\n    var w = 0;\n    for (var i=0;i<s.length;i++) w += (EM_W[s[i]] != null ? EM_W[s[i]] : 0.56);\n    return w;\n  }\n  var STROKE_SHORT = { freestyle:\"free\", backstroke:\"back\", breaststroke:\"breast\",\n                       butterfly:\"fly\", medley:\"IM\" };\n  function poolStroke(b){\n    var st = (b.item && b.item.strokes) || [];\n    if (st.length !== 1) return \"\";\n    return STROKE_SHORT[st[0]] || \"\";\n  }\n\n  // Spelled units. At 70 cm through glare a double prime and a single prime\n  // differ by one small stroke, so 30 seconds and 30 minutes look the same, and\n  // the pipe between the two halves is the first mark to wash out.\n  function restPhrase(r){\n    var s = String(r == null ? \"\" : r).trim();\n    if (!s) return \"\";\n    var sec = null, m;\n    if ((m = /^(\\d+)\\s*(?:\"|''|s|sec|secs|seconds)?$/i.exec(s))) sec = parseInt(m[1],10);\n    else if ((m = /^(\\d+)\\s*(?:'|min|mins|minute|minutes)$/i.exec(s))) sec = parseInt(m[1],10) * 60;\n    else if ((m = /^(\\d+)\\s*[:.]\\s*(\\d{1,2})$/.exec(s))) sec = parseInt(m[1],10)*60 + parseInt(m[2],10);\n    if (sec == null || !isFinite(sec)) return s;\n    if (sec < 60) return sec + \" sec\";\n    return Math.floor(sec/60) + \":\" + (sec % 60 < 10 ? \"0\" : \"\") + (sec % 60);\n  }\n  // His Adv 1 and Adv 2 split by rest is kept exactly. It just gets two rows\n  // instead of one line with a pipe in it.\n  function restRows(b){\n    var r1 = restPhrase(b.rest_adv1 || (b.item && (b.item.rest_adv1 || b.item.rest)));\n    var r2 = restPhrase(b.rest_adv2 || (b.item && (b.item.rest_adv2 || b.item.rest)));\n    if (r1 && r2 && r1 !== r2) return [[\"Adv 1\", r1], [\"Adv 2\", r2]];\n    var one = r1 || r2;\n    return one ? [[\"Rest\", one]] : [];\n  }\n\n  // One place for the warm-up wording. It used to be written out in four\n  // separate places, which is how four copies drift apart.\n  function warmupText(sk){\n    var S = data.settings || defaultSettings();\n    var mins = sk.warmup_minutes || S.warmup_minutes || 10;\n    return { role:\"Warm-up · \" + mins + \" min\",\n             call:\"Easy swim, own pace\",\n             watch:\"No exercises. Loosen up and let them talk.\" };\n  }\n  function endingText(sk){\n    var S = data.settings || defaultSettings();\n    var game = sk.endBlock && sk.endBlock.block_role === \"game\";\n    return (game ? \"Game · \" + (S.game_minutes||10) : \"Cool-down · \" + (S.cooldown_minutes||5)) + \" min\";\n  }\n  // The warm-up is a real saved row with a done column, so it can be ticked\n  // like anything else. It lives on the session object so a tick survives a\n  // repaint.\n  function warmBlock(sk){\n    var w = warmupText(sk);\n    if (!sk.warm) sk.warm = { block_role:\"warmup\", distance_m:0, done:!!sk.warmDone };\n    sk.warm.poolRole = w.role;\n    sk.warm.rendered_text = w.call;\n    sk.warm.watch_for = w.watch;\n    sk.warm.itemId = sk.warmItemId || null;\n    return sk.warm;\n  }\n  function poolOrder(sk){\n    var out = [warmBlock(sk)];\n    (sk.blocks||[]).forEach(function(b){ out.push(b); });\n    if (sk.endBlock){ sk.endBlock.poolRole = endingText(sk); out.push(sk.endBlock); }\n    return out;\n  }\n  function poolRole(b){\n    var name = b.poolRole || ROLE_LABEL[b.block_role] || b.block_role || \"\";\n    if (b.poolRole) return name;\n    return name + (b.distance_m ? \" · \" + b.distance_m + \" m\" : \"\");\n  }\n\n  // Whether he is on time, in words. A number to compare against another\n  // number is two fixations, this is one. Before anything is ticked the only\n  // honest reference is the hour itself, so it stays quiet until he runs over.\n  function poolDrift(sk, elapsedS, order){\n    var plannedS = (sk.planned_minutes || 60) * 60;\n    var doneM = 0, totalM = 0;\n    order.forEach(function(b){\n      var m = b.distance_m || 0;\n      totalM += m;\n      if (b.done) doneM += m;\n    });\n    var driftS;\n    if (doneM > 0 && totalM > 0) driftS = elapsedS - plannedS * (doneM / totalM);\n    else if (elapsedS > plannedS) driftS = elapsedS - plannedS;\n    else driftS = 0;\n    var mins = Math.round(driftS / 60);\n    if (mins > -2 && mins < 2) return { text:\"on time\", late:false };\n    if (mins > 0) return { text:mins + \" min behind\", late: mins > 5 };\n    return { text:(-mins) + \" min ahead\", late:false };\n  }\n\n  function poolKey(sk){ return String(sk.id || (sk.date + \"|\" + sk.slot)); }\n  function startPool(sk){\n    draft = sk; view.name = \"pool\";\n    // A clock that is already running for this session keeps running. Leaving\n    // pool view used to throw it away.\n    if (!poolState || poolState.key !== poolKey(sk)) poolState = { startedAt:Date.now(), key:poolKey(sk) };\n    render();\n  }\n  function leavePool(){\n    if (poolState && poolState.timer){ clearTimeout(poolState.timer); poolState.timer = null; }\n    poolEl = null; poolUndo = null;\n    view.name = \"today\"; render();\n  }\n  function askClosePool(sk){\n    openSheet(\"Finished?\", \"sm\", [\n      h(\"button\",{ onclick:function(){\n        closeSheet();\n        if (poolState && poolState.timer) clearTimeout(poolState.timer);\n        poolState = null;\n        markStatus(sk, \"done\"); view.name = \"today\";\n      } },\"Mark done and close\"),\n      h(\"button\",{ onclick:function(){ closeSheet(); leavePool(); } },\"Just close\"),\n      h(\"button\",{ onclick:closeSheet },\"Keep going\")\n    ]);\n  }\n\n  function renderPool(){\n    var sk = draft;\n    var order = poolOrder(sk);\n    var current = -1;\n    for (var ci=0; ci<order.length; ci++){ if (!order[ci].done){ current = ci; break; } }\n\n    var pool = h(\"div\",{ id:\"pool\",\n      class:(POOL_UI.big ? \"big\" : \"\") + (POOL_UI.night ? \" night\" : \"\") });\n    poolEl = pool;\n    pool.addEventListener(\"scroll\", function(){ poolScrollAt = Date.now(); });\n\n    var clock = h(\"div\",{ class:\"clock\", \"aria-hidden\":\"true\" },\"0:00\");\n    var drift = h(\"div\",{ class:\"drift\" },\"on time\");\n    var ptop = h(\"div\",{ class:\"ptop\", role:\"group\" },\n      h(\"div\",{class:\"pclockrow\"},\n        clock,\n        h(\"span\",{class:\"of\"}, \"of \" + (sk.planned_minutes||60) + \" min\"),\n        h(\"span\",{class:\"grow\"}),\n        h(\"button\",{ onclick:function(){ askClosePool(sk); } },\"Close\")),\n      drift);\n\n    var lastMin = -1, driftShown = null;\n    function tick(){\n      if (!poolState) return;\n      var el = Math.round((Date.now() - poolState.startedAt)/1000);\n      clock.textContent = mmss(el);\n      var d = poolDrift(sk, el, order);\n      // Only written when it actually changes. Rewriting the same string every\n      // second is work for nothing and it makes VoiceOver talk over him.\n      if (d.text !== driftShown){\n        driftShown = d.text;\n        drift.textContent = d.text;\n        drift.className = \"drift\" + (d.late ? \" late\" : \"\");\n      }\n      var mins = Math.floor(el/60);\n      if (mins !== lastMin){\n        lastMin = mins;\n        // Live digits would make VoiceOver read a new number every second, so\n        // the reading is announced on the minute only.\n        ptop.setAttribute(\"aria-label\",\n          mins + \" minutes gone of \" + (sk.planned_minutes||60) + \", \" + d.text);\n      }\n      poolState.timer = setTimeout(tick, 1000);\n    }\n    if (poolState && poolState.timer) clearTimeout(poolState.timer);\n    pool.append(ptop);\n    tick();\n\n    if (sk.goal) pool.append(h(\"div\",{class:\"pgoal\"}, sk.goal));\n    order.forEach(function(b, i){ pool.append(poolBlock(b, i === current)); });\n\n    var foot = h(\"div\",{class:\"pfoot\"},\n      h(\"button\",{ onclick:function(){ copyText(asPlainText(sk)); } },\"Copy\"));\n    if (poolUndo){\n      var u = poolUndo;\n      foot.append(h(\"button\",{ onclick:function(){\n        poolUndo = null; togglePool(u.block, null, true);\n      } }, \"Undo \" + u.label));\n    }\n    pool.append(foot);\n\n    document.body.append(pool);\n  }\n\n  function poolBlock(b, open){\n    var L = poolLines(b.rendered_text);\n    var stroke = poolStroke(b);\n    // Promote a stroke word onto the call line only when it still fits.\n    if (L.call && stroke && emWidth(L.call + \" \" + stroke) <= 5.8) L.call = L.call + \" \" + stroke;\n    var rows = restRows(b);\n    var cue = b.cue || (b.item && b.item.cue) || (b.game && b.game.how_it_works) || b.watch_for || \"\";\n\n    var main = h(\"div\",{class:\"pmain\"},\n      h(\"div\",{class:\"prole\"}, poolRole(b)),\n      L.call ? h(\"div\",{class:\"pcall\"}, L.call) : null,\n      rows.length ? h(\"div\",{class:\"prest\"}, rows.map(function(r){\n        return h(\"div\",null, h(\"span\",{class:\"rlab\"}, r[0]), r[1]);\n      })) : null,\n      L.qual ? h(\"div\",{class: L.call ? \"pqual\" : \"pqual lead\"}, L.qual) : null,\n      cue ? h(\"div\",{class:\"pcue\"}, cue) : null,\n      advRows(b));\n\n    var label = L.call || L.qual;\n    var strip = h(\"button\",{ class:\"ptick\" + (b.done ? \" on\" : \"\"),\n      \"aria-pressed\": b.done ? \"true\" : \"false\",\n      \"aria-label\": (b.done ? \"Done, tap to undo: \" : \"Mark done: \") + label },\n      h(\"span\",{class:\"mk\"}, b.done ? \"✓\" : \"\"));\n    armTick(strip, b, label);\n\n    return h(\"div\",{ class:\"pblk\" + (b.done ? \" done\" : \"\") + (open ? \"\" : \" collapsed\") },\n      h(\"div\",{class:\"prow\"}, main, strip));\n  }\n  function advRows(b){\n    var a1 = advText(b,1), a2 = advText(b,2);\n    if (!a1 && !a2) return null;\n    return h(\"div\",{class:\"padv\"},\n      a1 ? h(\"div\",null, h(\"span\",{class:\"rlab\"},\"Adv 1\"), a1) : null,\n      a2 ? h(\"div\",null, h(\"span\",{class:\"rlab\"},\"Adv 2\"), a2) : null);\n  }\n\n  // Geometry and timing only. Contact area and pressure are not usable signals:\n  // iOS reports a constant width and height for a touch, and force is 0 on a\n  // phone without 3D Touch.\n  function armTick(strip, b, label){\n    var down = null;\n    strip.addEventListener(\"pointerdown\", function(e){\n      if (!e.isPrimary) return;\n      down = { x:e.clientX, y:e.clientY, t:Date.now() };\n    });\n    strip.addEventListener(\"pointercancel\", function(){ down = null; });\n    strip.addEventListener(\"pointerup\", function(e){\n      var d = down; down = null;\n      if (!d || !e.isPrimary) return;\n      var moved = Math.abs(e.clientX - d.x) + Math.abs(e.clientY - d.y);\n      var dwell = Date.now() - d.t;\n      if (moved > 10) return;                                    // a wet slip is a drag, not a tap\n      if (dwell < 40 || dwell > 1200) return;                    // a droplet spike, or a hand resting on the phone\n      if (Date.now() - poolScrollAt < 350) return;               // the rule that earns its keep\n      if (Date.now() - poolTickAt < 400) return;\n      if (b.tickedAt && Date.now() - b.tickedAt < 1500) return;  // water can bridge two contacts and fire twice\n      poolTickAt = Date.now();\n      togglePool(b, label);\n    });\n    // A keyboard or a VoiceOver tap is never a wet finger, so it skips the gate.\n    strip.addEventListener(\"keydown\", function(e){\n      if (e.key === \"Enter\" || e.key === \" \"){ e.preventDefault(); togglePool(b, label); }\n    });\n  }\n\n  // The check is painted before the database write, because the write can be\n  // slow and on the deck it is often offline.\n  async function togglePool(b, label, isUndo){\n    var keep = poolEl ? poolEl.scrollTop : 0;\n    b.done = !b.done;\n    b.tickedAt = Date.now();\n    poolUndo = isUndo ? null : { block:b, label:label || \"\" };\n    renderPool();\n    if (poolEl) poolEl.scrollTop = keep;\n    poolClick();\n    if (b.itemId && sb && session && !offline){\n      try{ await sb.from(\"tr_session_items\").update({ done:b.done }).eq(\"id\", b.itemId); }catch(e){}\n    }\n  }",
    "risk": "The highest risk edit in the plan, and all of it sits in armTick. Tested: the suite passes, and the probe paints renderPool three ways, including with blocks ticked and with a game ending. Everything is inside the confirmed allowlist. AudioContext and webkitAudioContext are behind typeof guards, so the top level cannot throw a ReferenceError. No requestAnimationFrame, no matchMedia, no scrollIntoView. poolEl.scrollTop is a plain property. It still has to be tried with wet hands on his phone before anything else lands."
  },
  {
    "what": "renderSessionCard puts the session first: title, one status line, the goal, then the 56px pool button above the fold. The 2x2 tile grid becomes a 44px stat strip, which is about 158px back. Provenance and relaxations drop to a footnote, only the safety checks stay above the card as a banner. Six competing buttons become a grouped action list.",
    "where": "renderSessionCard(), lines 1559 to 1610",
    "before": "  function renderSessionCard(sk, editable){\n    if (sk.fallbackNote) APP.append(h(\"div\",{class:\"banner\"}, sk.fallbackNote));\n    (sk.relaxations || []).forEach(function(r){ APP.append(h(\"div\",{class:\"banner\"}, r)); });\n    (sk.warnings || []).forEach(function(w){ APP.append(h(\"div\",{class:\"banner\"}, \"Check this: \" + w)); });",
    "after": "  function renderSessionCard(sk, editable){\n    // Safety checks are the only messages that should stop the eye, so they are\n    // the only ones still above the card.\n    (sk.warnings || []).forEach(function(w){\n      APP.append(h(\"div\",{class:\"banner warn\", role:\"alert\"}, \"Check this: \" + w)); });\n\n    APP.append(h(\"div\",{class:\"card\"},\n      h(\"h2\",null, sk.title),\n      h(\"div\",{class:\"small muted\"},\n        [ sk.slot && sk.slot !== \"other\" ? slotLabel(sk.slot) : null,\n          fmtM(sk.planned_total_m||0),\n          (sk.planned_minutes||60) + \" min planned\"\n        ].filter(Boolean).join(\" · \")),\n      sk.status === \"done\" ? h(\"div\",{class:\"row wrap\",style:{marginTop:\"8px\"}},\n        h(\"span\",{class:\"badge ok\"},\"done\")) : null,\n      sk.status === \"skipped\" ? h(\"div\",{class:\"row wrap\",style:{marginTop:\"8px\"}},\n        h(\"span\",{class:\"badge warn\"},\"did not run\")) : null,\n      h(\"div\",{class:\"goalbox\",style:{marginTop:\"12px\"}},\n        h(\"div\",{class:\"lbl\"},\"Today's goal\"),\n        h(\"div\",{class:\"txt\"}, sk.goal)),\n      sk.fallbackNote ? h(\"div\",{class:\"banner info\"}, sk.fallbackNote) : null,\n      (sk.relaxations||[]).length ? h(\"div\",{class:\"tiny muted\"}, sk.relaxations.join(\" \")) : null,\n      h(\"div\",{class:\"tiny muted\"}, sk.engine === \"ai\" ? \"Coaching notes by Claude.\" : \"Built from the library.\"),\n      h(\"button\",{class:\"primary big\",style:{marginTop:\"12px\"},\n        onclick:function(){ startPool(sk); }},\n        poolState && poolState.key === poolKey(sk) ? \"Back to pool view\" : \"Start pool view\")));\n\n    APP.append(h(\"div\",{class:\"strip\"},\n      stat(fmtM(sk.planned_total_m||0), \"Total\"),\n      stat((sk.planned_minutes||60) + \" min\", \"Planned\"),\n      stat(String((sk.blocks||[]).length), \"Blocks\"),\n      stat(sk.ending === \"game\" ? \"Game\" : \"Cool-down\", \"Ending\")));\n\n    var w = warmupText(sk);\n    APP.append(h(\"div\",{class:\"blk\"},\n      h(\"div\",{class:\"role\"}, w.role),\n      h(\"div\",{class:\"call\"}, w.call),\n      h(\"div\",{class:\"watch\"}, w.watch)));\n\n    (sk.blocks||[]).forEach(function(b, i){ APP.append(blockCard(b, i, sk, editable)); });\n\n    if (sk.endBlock){\n      var e = sk.endBlock;\n      var eL = poolLines(e.rendered_text);\n      APP.append(h(\"div\",{class:\"blk\"},\n        h(\"div\",{class:\"role\"}, endingText(sk)),\n        h(\"div\",{class:\"call\"}, eL.call || eL.qual),\n        eL.call && eL.qual ? h(\"div\",{class:\"qual\"}, eL.qual) : null,\n        (e.cue || (e.item && e.item.cue)) ? h(\"div\",{class:\"cue\"}, e.cue || e.item.cue) : null,\n        (e.watch_for || (e.game && e.game.how_it_works)) ?\n          h(\"div\",{class:\"watch\"}, e.watch_for || e.game.how_it_works) : null));\n    }\n\n    APP.append(listCard(\"Actions\", [\n      editable && !sk.saved && !offline ? irow({ title:\"Save\", tone:\"good\", act:true,\n        onclick:function(){ saveDraft(null); } }) : null,\n      sk.saved ? irow({ title:\"Mark done\", act:true,\n        onclick:function(){ markStatus(sk,\"done\"); } }) : null,\n      sk.saved ? irow({ title:\"Did not run\", act:true,\n        onclick:function(){ markStatus(sk,\"skipped\"); } }) : null,\n      editable ? irow({ title:\"Regenerate\", act:true,\n        onclick:function(){ askRegenerate(sk); } }) : null,\n      irow({ title:\"Share\", act:true, onclick:function(){ askShare(sk); } })\n    ]));\n\n    if (sk.saved) APP.append(feedbackCard(sk));\n  }\n  // Print moves out of the main row: it is a prep at home action, not a\n  // poolside one.\n  function askShare(sk){\n    openSheet(\"Share\", \"sm\", [\n      h(\"button\",{class:\"wide\",onclick:function(){ closeSheet(); copyText(asPlainText(sk)); }},\"Copy as text\"),\n      h(\"button\",{class:\"wide\",style:{marginTop:\"12px\"},onclick:function(){ closeSheet(); window.print(); }},\"Print\")\n    ]);\n  }\n  // The alert on save already promised this control, and replace:true was\n  // produced nowhere in the file, so there was no way to redo a saved session.\n  function askRegenerate(sk){\n    openSheet(\"Replace this training?\", \"sm\", [\n      h(\"div\",{class:\"small muted\"},\"The sets go back in the rotation and a new session is built for the same date.\"),\n      h(\"button\",{class:\"primary wide\",style:{marginTop:\"12px\"},onclick:function(){\n        closeSheet(); generateToday(todayOpts(true)); }},\"Replace it\"),\n      h(\"button\",{class:\"wide\",style:{marginTop:\"12px\"},onclick:closeSheet},\"Keep this one\")\n    ]);\n  }",
    "risk": "Replace the whole function body down to and including the old `if (sk.saved) APP.append(feedbackCard(sk));` and its closing brace. renderSessionCard is exported as renderCard and is the hook the harness smoke test uses, so a throw here is caught by the suite once harness commit 1 has landed. Tested: the probe paints it saved and unsaved. Depends on warmupText, endingText and poolLines from the pool edit, so that edit must land first. Keep tile() in the file, Insights still uses it until its own edit."
  },
  {
    "what": "blockCard splits the set line the same way pool view does, moves rest onto its own line, and replaces the two 26.6px rep nudge buttons with a 44px Adjust reps row that works whether or not the session is saved. Safety tags stop rendering raw database strings.",
    "where": "blockCard(), lines 1617 to 1640",
    "before": "    return h(\"div\",{class:\"blk\"},\n      h(\"div\",{class:\"role\"}, (ROLE_LABEL[b.block_role] || b.block_role) +\n        (b.distance_m ? \" · \" + b.distance_m + \" m\" : \"\")),\n      h(\"div\",{class:\"line\"}, b.rendered_text + (rest ? \", \" + rest : \"\")),",
    "after": "    var L = poolLines(b.rendered_text);\n    var rrows = restRows(b);\n    return h(\"div\",{class:\"blk\"},\n      h(\"div\",{class:\"role\"}, (ROLE_LABEL[b.block_role] || b.block_role) +\n        (b.distance_m ? \" · \" + b.distance_m + \" m\" : \"\")),\n      h(\"div\",{class:\"call\"}, L.call || L.qual),\n      rrows.length ? h(\"div\",{class:\"rest\"}, rrows.map(function(r){\n        return r[0] + \" \" + r[1]; }).join(\", \")) : null,\n      L.call && L.qual ? h(\"div\",{class:\"qual\"}, L.qual) : null,",
    "risk": "Delete the now unused `var rest = restText(b),` from the head of the function, keeping a1 and a2. At the end of the same function, replace the rep nudge span in the meta row with a footer row: `h(\"div\",{class:\"meta\"}, ...badges only...), b.item && b.item.reps ? h(\"div\",{class:\"blkact\"}, irow({ title:\"Adjust reps\", act:true, onclick:function(){ askReps(sk, i); } })) : null);` and change the safety tag map to `SAFETY_LABEL[t] || t.replace(/_/g,\" \")`, which also fixes the old single replace that only handled the first underscore. Add at top level: `// Raw strings like max_effort are not how he talks. Confirm this list with him\\n  // before it ships, the underwater one especially.\\n  var SAFETY_LABEL = { max_effort:\"all out, watch them\", breath_control:\"breath control set\", underwater:\"underwater work\", hypoxic:\"limited breathing\", sprint:\"sprint set\" };` and `function askReps(sk, i){ var S = data.settings || defaultSettings(); var b = sk.blocks[i]; function step(d){ nudge(sk, i, d); askReps(sk, i); } openSheet(\"Adjust reps\",\"sm\",[ h(\"div\",{class:\"small muted\"}, b.rendered_text + \", \" + fmtM(costBlocks(sk.blocks, S).metres) + \" in the main body\"), h(\"div\",{class:\"row\",style:{marginTop:\"12px\",gap:\"16px\"}}, h(\"button\",{class:\"big\",onclick:function(){ step(-1); }},\"− one rep\"), h(\"button\",{class:\"big\",onclick:function(){ step(1); }},\"+ one rep\")) ]); }`. Reopening the sheet after each step is what refreshes the metre total, since nudge calls render and the sheet is outside APP."
  },
  {
    "what": "The ending block in the text export reads its minutes from settings instead of hard-coding 10 and 5. Only the number changes. The surrounding format is asserted by verify check 12.",
    "where": "asPlainText(), line 1366",
    "before": "      L.push((e.block_role === \"game\" ? \"Game (10 min)\" : \"Cool-down (5 min)\") + \": \" + e.rendered_text);",
    "after": "      var eS = data.settings || defaultSettings();\n      L.push((e.block_role === \"game\" ? \"Game (\" + (eS.game_minutes||10) + \" min)\"\n                                      : \"Cool-down (\" + (eS.cooldown_minutes||5) + \" min)\") + \": \" + e.rendered_text);",
    "risk": "Check 12 asserts txt.includes(\"Goal:\"), txt.includes(\"Warm-up\"), /Total .* planned \\d+ min/ and no em or en dashes anywhere in the output. All four still hold, because only the digits inside the brackets move. Tested: the suite passes."
  },
  {
    "what": "renderToday answers what today's session is, then how to start it. The four inline selects become a grouped list of four rows that open sheets. The generating state gets a progress line on the button and three skeleton blocks, because a disabled button on its own looks broken for 30 seconds.",
    "where": "renderToday(), lines 1511 to 1557",
    "before": "  function renderToday(){\n    var S = data.settings || defaultSettings();\n    var dateInput = h(\"input\",{ type:\"date\", value: (draft && draft.date) || nextClassISO() });",
    "after": "  function renderToday(){\n    var S = data.settings || defaultSettings();\n    if (!setup.date) setup.date = (draft && draft.date) || nextClassISO();\n    if (!setup.slot) setup.slot = (draft && draft.slot) || slotForDate(setup.date);\n    if (!setup.ending) setup.ending = (draft && draft.ending) || S.default_ending || \"cooldown\";\n\n    if (draft){\n      setTitle(fmtDayDate(draft.date), draft.title);\n      setBar(BARL, \"Settings\", openSettings);\n      setBar(BARR, \"Pool\", function(){ startPool(draft); });\n      renderSessionCard(draft, true);\n      return;\n    }\n\n    setTitle(\"Today\", \"Today\");\n    setBar(BARL, \"Settings\", openSettings);\n\n    APP.append(listCard(\"Next session\", [\n      irow({ title:\"Date\", value:fmtDate(setup.date), onclick:function(){ askSetup(\"date\"); } }),\n      irow({ title:\"Slot\", value:slotLabel(setup.slot), onclick:function(){ askSetup(\"slot\"); } }),\n      irow({ title:\"Ending\", value:setup.ending === \"game\" ? \"10 min game\" : \"5 min cool-down\",\n             onclick:function(){ askSetup(\"ending\"); } }),\n      irow({ title:\"Focus\", value:focusName(setup.focusKey), onclick:function(){ askSetup(\"focus\"); } })\n    ]));\n\n    APP.append(h(\"button\",{ class:\"primary big\" + (generating ? \" working\" : \"\"),\n      disabled: generating || busy,\n      onclick:function(){ generateToday(todayOpts(false)); } },\n      generating ? \"Writing today's training…\" : \"Generate today's training\"));\n\n    if (generating){\n      APP.append(h(\"div\",{class:\"small muted\",style:{marginTop:\"12px\"}},\n        \"Picking the focus, filling the 45 minutes, then Claude writes the coaching notes. About 30 seconds.\"));\n      APP.append(h(\"div\",{class:\"skel a\",style:{marginTop:\"16px\"}}));\n      APP.append(h(\"div\",{class:\"skel b\"}));\n      APP.append(h(\"div\",{class:\"skel c\"}));\n      return;\n    }\n    // The old line promised no repeat inside six sessions, which is not\n    // literally true: effectiveWindow shrinks the window to half the role pool.\n    APP.append(h(\"div\",{class:\"empty\"},\n      \"Press the button. The app picks a focus you have not done recently, fills the 45 minutes from the library, and keeps a set out of the rotation for as long as the library allows.\"));\n  }\n  function focusName(k){\n    if (!k) return \"App chooses\";\n    var f = data.focuses.filter(function(x){ return x.key === k; })[0];\n    return f ? f.name : \"App chooses\";\n  }\n  function askSetup(which){\n    if (which === \"date\"){\n      var d = h(\"input\",{ type:\"date\", value:setup.date, \"aria-label\":\"Session date\" });\n      d.addEventListener(\"change\", function(){\n        setup.date = d.value; setup.slot = slotForDate(d.value); });\n      openSheet(\"Date\",\"sm\",[ d,\n        h(\"button\",{class:\"primary wide\",style:{marginTop:\"16px\"},\n          onclick:function(){ closeSheet(); render(); }},\"Done\") ]);\n      return;\n    }\n    if (which === \"slot\"){\n      openSheet(\"Slot\",\"sm\", sheetList(SLOTS.map(function(s){\n        return irow({ title:s.label, tone: s.key === setup.slot ? \"accent\" : \"\",\n          onclick:function(){ setup.slot = s.key; closeSheet(); render(); } });\n      })));\n      return;\n    }\n    if (which === \"ending\"){\n      openSheet(\"Ending\",\"sm\", sheetList([\n        irow({ title:\"Finish with a 5 min cool-down\", tone: setup.ending === \"cooldown\" ? \"accent\" : \"\",\n          onclick:function(){ setup.ending = \"cooldown\"; closeSheet(); render(); } }),\n        irow({ title:\"Finish with a 10 min game\", tone: setup.ending === \"game\" ? \"accent\" : \"\",\n          onclick:function(){ setup.ending = \"game\"; closeSheet(); render(); } })\n      ]));\n      return;\n    }\n    openSheet(\"Focus\",\"med\", sheetList([irow({ title:\"Let the app choose\", sub:\"Recommended\",\n        tone: setup.focusKey ? \"\" : \"accent\",\n        onclick:function(){ setup.focusKey = \"\"; closeSheet(); render(); } })].concat(\n      data.focuses.map(function(f){\n        return irow({ title:f.name, tone: setup.focusKey === f.key ? \"accent\" : \"\",\n          onclick:function(){ setup.focusKey = f.key; closeSheet(); render(); } });\n      }))));\n  }",
    "risk": "Replace the whole function, lines 1511 to 1557 inclusive. The \"six sessions\" copy change is one of the five items to clear with Vlad first; if he prefers the old sentence, keep it verbatim instead. The generate button no longer says \"Generate another\", because with a draft on screen the whole no-draft branch is skipped and Regenerate is the path."
  },
  {
    "what": "History becomes grouped inset lists, the search term survives a repaint, a row pushes a session detail screen instead of overwriting Today, and the Quick look card goes.",
    "where": "renderHistory(), lines 1767 to 1811",
    "before": "  function renderHistory(){\n    var q = h(\"input\",{ type:\"search\", placeholder:\"Search titles and sets\" });\n    var box = h(\"div\",{class:\"card\"});",
    "after": "  function renderHistory(){\n    setTitle(\"History\",\"History\");\n    setBar(BARR, \"Search\", askHistorySearch);\n\n    var term = (hist.q||\"\").toLowerCase();\n    var rows = data.sessions.filter(function(s){\n      if (!term) return true;\n      if ((s.title||\"\").toLowerCase().indexOf(term) !== -1) return true;\n      return (data.items[s.id]||[]).some(function(i){\n        return (i.rendered_text||\"\").toLowerCase().indexOf(term) !== -1; });\n    });\n    if (hist.q) APP.append(h(\"div\",{class:\"banner info\"}, \"Showing sessions that match \" + hist.q + \".\"));\n    if (!rows.length){\n      APP.append(h(\"div\",{class:\"empty\"}, hist.q ? \"Nothing matches that.\" : \"Nothing saved yet.\"));\n      return;\n    }\n\n    var week = null, bucket = [];\n    function flush(){ if (bucket.length) APP.append(listCard(week, bucket)); bucket = []; }\n    rows.forEach(function(s){\n      var wk = weekLabel(s.session_date);\n      if (wk !== week){ flush(); week = wk; }\n      bucket.push(irow({\n        date: fmtDate(s.session_date),\n        title: s.title,\n        foot: [ s.slot && s.slot !== \"other\" ? slotLabel(s.slot) : null,\n                fmtM(s.planned_total_m||0),\n                s.status === \"done\" ? \"done\" : (s.status === \"skipped\" ? \"did not run\" : \"planned\")\n              ].filter(Boolean).join(\" · \"),\n        badges: s.difficulty_rating ? [h(\"span\",{class:\"badge\"}, s.difficulty_rating + \"/5\")] : [],\n        onclick: function(){\n          view.session = hydrate(s); view.from = \"history\"; view.name = \"session\";\n          render(); window.scrollTo(0,0);\n        }\n      }));\n    });\n    flush();\n  }\n  function askHistorySearch(){\n    var q = h(\"input\",{ type:\"search\", value:hist.q, placeholder:\"Search titles and sets\",\n      \"aria-label\":\"Search titles and sets\" });\n    openSheet(\"Search\",\"sm\",[ q,\n      h(\"button\",{class:\"primary wide\",style:{marginTop:\"16px\"},onclick:function(){\n        hist.q = q.value; closeSheet(); render(); }},\"Search\"),\n      hist.q ? h(\"button\",{class:\"wide\",style:{marginTop:\"12px\"},onclick:function(){\n        hist.q = \"\"; closeSheet(); render(); }},\"Clear\") : null ]);\n  }",
    "risk": "Replace the whole function down to and including the Quick look card, lines 1767 to 1811. weekLabel at 1812 stays. The Quick look button promised something the code did not deliver: it took the most recent Thursday session, not last Thursday, and the first row of the list is usually that session anyway."
  },
  {
    "what": "Library becomes grouped lists with a set detail sheet. The 26.6px Add and No pair four pixels apart is gone, Retire loses its browser confirm, retired sets are reachable through a Retired filter and can be brought back, and Ask Claude gets a sheet with a role, a focus and a count instead of a bare button.",
    "where": "renderLibrary() and its helpers, lines 1820 to 1953",
    "before": "  function toggleActive(x){\n    if (!confirm(\"Retire \\\"\" + x.name + \"\\\"? It stops coming up in new trainings.\")) return;\n    mutate(function(){ return sb.from(\"tr_exercises\").update({ active:false }).eq(\"id\", x.id); });\n  }",
    "after": "  function setActive(x, on){\n    x.active = on;\n    mutate(function(){ return sb.from(\"tr_exercises\").update({ active:on }).eq(\"id\", x.id); });\n  }\n  function askRetire(x){\n    openSheet(\"Retire this set?\",\"sm\",[\n      h(\"div\",{class:\"small muted\"},\"It stays in your saved trainings. It just stops coming up in new ones.\"),\n      h(\"button\",{class:\"danger wide\",style:{marginTop:\"16px\"},onclick:function(){\n        closeSheet(); setActive(x,false); toast(\"Retired.\"); }},\"Retire it\"),\n      h(\"button\",{class:\"wide\",style:{marginTop:\"12px\"},onclick:closeSheet},\"Keep it\")\n    ]);\n  }\n  // The only place in the app that shows a set in full. It is also the whole\n  // reason the Add and No pair can go: the decision now has a screen of its own.\n  function openSetSheet(x, proposed){\n    var kids = [\n      h(\"div\",{class:\"small\"}, x.structure + (x.rest ? \", \" + x.rest : \"\")),\n      x.cue ? h(\"div\",{class:\"callout\",style:{marginTop:\"8px\",color:\"var(--accent-text)\"}}, x.cue) : null,\n      x.watch_for ? h(\"div\",{class:\"small muted\",style:{marginTop:\"6px\"}}, x.watch_for) : null,\n      h(\"div\",{class:\"tiny muted\",style:{marginTop:\"8px\"}},\n        [ ROLE_LABEL[x.role]||x.role, (x.focus_tags||[]).join(\", \"),\n          (x.equipment||[]).join(\" + \") || \"no gear\",\n          (x.safety_tags||[]).map(function(t){ return SAFETY_LABEL[t] || t.replace(/_/g,\" \"); }).join(\", \")\n        ].filter(Boolean).join(\" · \")),\n      x.steady_variant ? h(\"div\",{class:\"small\",style:{marginTop:\"8px\"}},\"Adv 1: \" + x.steady_variant) : null,\n      x.strong_variant ? h(\"div\",{class:\"small\"},\"Adv 2: \" + x.strong_variant) : null\n    ];\n    if (proposed){\n      kids.push(h(\"button\",{class:\"primary big\",style:{marginTop:\"20px\"},onclick:function(){\n        closeSheet(); setStatus(x,\"active\"); toast(\"Added to the library.\"); }},\"Add to library\"));\n      // 48px down, because one of these is easy to undo and one is not.\n      kids.push(h(\"button\",{class:\"danger wide\",style:{marginTop:\"48px\"},onclick:function(){\n        closeSheet(); setStatus(x,\"rejected\"); toast(\"Left out.\"); }},\"Not this one\"));\n    } else if (x.active === false){\n      kids.push(h(\"button\",{class:\"primary big\",style:{marginTop:\"20px\"},onclick:function(){\n        closeSheet(); setActive(x,true); toast(\"Back in the rotation.\"); }},\"Bring back\"));\n    } else {\n      kids.push(h(\"button\",{class:\"danger wide\",style:{marginTop:\"20px\"},onclick:function(){\n        closeSheet(); askRetire(x); }},\"Retire\"));\n    }\n    openSheet(x.name, \"med\", kids);\n  }\n  function askLibFilter(){\n    var q = h(\"input\",{type:\"search\",value:lib.q,placeholder:\"Search the library\",\n      \"aria-label\":\"Search the library\"});\n    var roles = {};\n    data.library.forEach(function(x){ if (isActive(x)) roles[x.role] = (roles[x.role]||0)+1; });\n    function pick(r){ return function(){ lib.role = r; lib.q = q.value; closeSheet(); render(); }; }\n    var rows = [ irow({ title:\"All roles\", tone: lib.role ? \"\" : \"accent\", onclick:pick(\"\") }) ];\n    Object.keys(roles).sort().forEach(function(r){\n      rows.push(irow({ title:ROLE_LABEL[r]||r, value:String(roles[r]),\n        tone: lib.role === r ? \"accent\" : \"\", onclick:pick(r) }));\n    });\n    rows.push(irow({ title:\"Retired\", tone: lib.role === \"retired\" ? \"accent\" : \"\", onclick:pick(\"retired\") }));\n    openSheet(\"Filter\",\"med\",[ q, h(\"div\",{style:{marginTop:\"16px\"}}, sheetList(rows)) ]);\n  }\n  // growLibrary used to pick the role from the thinnest one and the focus at\n  // random from every focus including inactive ones, with no way to see either.\n  function askClaude(prefRole){\n    var thin = thinRoles();\n    var role = prefRole || (thin.length ? thin[0].role : \"main_set\");\n    var focus = leastUsedFocus();\n    var count = 5;\n    function paint(){\n      openSheet(\"Ask Claude\",\"med\",[\n        sheetList([\n          irow({ title:\"Role\", value:ROLE_LABEL[role]||role, onclick:function(){\n            openSheet(\"Role\",\"med\", sheetList(Object.keys(ROLE_LABEL).map(function(r){\n              return irow({ title:ROLE_LABEL[r], tone: r === role ? \"accent\" : \"\",\n                onclick:function(){ role = r; paint(); } });\n            }))); } }),\n          irow({ title:\"Focus\", value: focus ? focus.name : \"Any\", onclick:function(){\n            openSheet(\"Focus\",\"med\", sheetList(data.focuses.map(function(f){\n              return irow({ title:f.name, tone: focus && focus.key === f.key ? \"accent\" : \"\",\n                onclick:function(){ focus = f; paint(); } });\n            }))); } }),\n          irow({ title:\"How many\", value:String(count) })\n        ]),\n        h(\"div\",{class:\"row\",style:{marginTop:\"12px\",gap:\"16px\"}},\n          h(\"button\",{class:\"big\",\"aria-label\":\"Fewer sets\",\n            onclick:function(){ count = Math.max(1,count-1); paint(); }},\"−\"),\n          h(\"button\",{class:\"big\",\"aria-label\":\"More sets\",\n            onclick:function(){ count = Math.min(10,count+1); paint(); }},\"+\")),\n        h(\"button\",{class:\"primary big\",style:{marginTop:\"20px\"},disabled: busy || offline,\n          onclick:function(){ closeSheet(); growLibrary(role, focus, count); }},\"Ask Claude\"),\n        h(\"div\",{class:\"tiny muted\",style:{marginTop:\"12px\"}},\n          \"The generator only uses active sets. Retiring one keeps it in your saved trainings but stops it coming up again.\")\n      ]);\n    }\n    paint();\n  }\n  function leastUsedFocus(){\n    var seen = {};\n    data.sessions.forEach(function(s){ seen[s.focus_key] = true; });\n    var never = data.focuses.filter(function(f){ return !seen[f.key]; });\n    return never[0] || data.focuses[0] || null;\n  }",
    "risk": "This entry replaces toggleActive and adds the sheets. In the same edit, replace renderLibrary itself with a version that calls setTitle(\"Library\",\"Library\"), setBar(BARL,\"Filter\",askLibFilter), setBar(BARR,\"Add\",function(){ askClaude(null); }), renders a three cell strip of active, waiting and retired counts, then listCard(\"Claude suggests\", ...) whose rows call openSetSheet(x,true), then the filtered rows as one listCard whose rows call openSetSheet(x,false). The row filter keeps the isActive fix from the earlier edit plus one new branch at the top: `if (lib.role === \"retired\") return x.active === false;`. Also change growLibrary's signature to `async function growLibrary(role, focus, count){ role = role || \"main_set\"; count = count || 5;` and delete the three lines that used to pick them, and change line 2010 in Insights to `onclick:function(){ askClaude(t.role); }`. Nothing here is asserted by verify."
  },
  {
    "what": "Insights puts the card he acts on first, lists the never used sets instead of throwing the array away, and gets the empty state it never had. Settings leave this screen for a sheet behind the gear.",
    "where": "renderInsights(), lines 1972 to 2036",
    "before": "  function renderInsights(){\n    var recent = data.sessions.filter(function(s){ return s.status !== \"skipped\"; }).slice(0, 24);",
    "after": "  function renderInsights(){\n    setTitle(\"Insights\",\"Insights\");\n    setBar(BARR, \"Settings\", openSettings);\n\n    if (!data.sessions.length){\n      APP.append(h(\"div\",{class:\"empty\"},\n        \"Nothing to show yet. Save a couple of trainings and this fills in.\"));\n      return;\n    }\n    var recent = data.sessions.filter(function(s){ return s.status !== \"skipped\"; }).slice(0, 24);",
    "risk": "Then, inside the same function: replace the four tiles with a four cell .strip using stat(); move the thin roles block above the focus balance card and make each row `irow({ title:ROLE_LABEL[t.role]||t.role, badges:[h(\"span\",{class:\"badge warn\"}, t.have + \" of \" + t.need)], onclick:function(){ askClaude(t.role); } })` inside listCard(\"The pool is getting thin\", ...), keeping the sentence about repeating sooner than six sessions verbatim as a footnote under it; give Never used and Most used their own listCard each, with the never used rows actually listed and opening openSetSheet; and delete the Settings card at 2028 to 2035 along with settingRow at 2037 to 2047. The old stale variable at 2014 was computed and thrown away."
  },
  {
    "what": "The Settings sheet. Five editable settings become 44px stepper rows, the four switches appear, and the account row takes over from #who, which is then deleted along with renderWho.",
    "where": "new function near the Insights section, plus deletions at lines 174, 1426 to 1434 and 1442",
    "before": "  function renderWho(){\n    clear(WHO);\n    if (!sb || !session) return;\n    var name = coachRow ? coachRow.display_name : session.user.email;\n    WHO.append(\n      h(\"span\",{class:\"small muted\",style:{marginRight:\"8px\"}}, name),\n      h(\"button\",{class:\"ghost small\", onclick:signOut }, \"Log out\")\n    );\n  }",
    "after": "  // Settings sits behind a bar button, not a fifth tab. He opens it a few times\n  // a year and a fifth slot would steal thumb width from four he uses weekly.\n  function openSettings(){\n    var name = coachRow ? coachRow.display_name : (session ? session.user.email : \"\");\n    openSheet(\"Settings\",\"large\",[\n      h(\"div\",{class:\"list-hdr\"},\"Account\"),\n      sheetList([\n        irow({ title:name, sub: session ? session.user.email : \"\" }),\n        irow({ title:\"Log out\", tone:\"danger\", onclick:function(){ closeSheet(); signOut(); } })\n      ]),\n      h(\"div\",{class:\"list-hdr\",style:{marginTop:\"28px\"}},\"Pace\"),\n      sheetList([\n        stepRow(\"Steady pace, seconds per 100 m (includes rest and you talking)\",\"pace_steady_s_per_100\",5),\n        stepRow(\"Faster pace, seconds per 100 m\",\"pace_strong_s_per_100\",5)\n      ]),\n      h(\"div\",{class:\"tiny muted\",style:{padding:\"7px 16px 0\"}},\n        \"Change the two pace numbers after a couple of real sessions. They are what makes the clock come out right.\"),\n      h(\"div\",{class:\"list-hdr\",style:{marginTop:\"28px\"}},\"Generator\"),\n      sheetList([\n        stepRow(\"Main block, least metres\",\"main_min_m\",50),\n        stepRow(\"Main block, most metres\",\"main_max_m\",50),\n        stepRow(\"Sessions before a set can come back\",\"no_repeat_sessions\",1)\n      ]),\n      h(\"div\",{class:\"list-hdr\",style:{marginTop:\"28px\"}},\"Pool view\"),\n      sheetList([\n        switchRow(\"Bigger text\", POOL_UI.big, function(v){ POOL_UI.big = v; }),\n        switchRow(\"Night pool\", POOL_UI.night, function(v){ POOL_UI.night = v; }),\n        switchRow(\"Tick sound\", POOL_UI.sound, function(v){ POOL_UI.sound = v; })\n      ]),\n      h(\"div\",{class:\"tiny muted\",style:{padding:\"7px 16px 0\"}},\n        \"Pool view stays white in the dark on purpose. A big light field is easier to read at arm's length, and drops of water show up worse on black.\"),\n      h(\"div\",{class:\"list-hdr\",style:{marginTop:\"28px\"}},\"Display\"),\n      sheetList([\n        switchRow(\"Solid backgrounds\", bodyFlags.solid, function(v){ bodyFlags.solid = v; paintBody(); })\n      ])\n    ]);\n  }\n  function stepRow(label, key, step){\n    var S = data.settings || defaultSettings();\n    var val = h(\"span\",{class:\"val\"}, String(S[key]));\n    function bump(d){\n      var v = parseInt(val.textContent,10) + d*step;\n      if (!isFinite(v) || v < 0) return;\n      val.textContent = String(v);\n      if (data.settings) data.settings[key] = v;\n      var patch = {}; patch[key] = v;\n      mutate(function(){ return sb.from(\"tr_settings\").update(patch).eq(\"id\",1); });\n    }\n    return h(\"div\",{class:\"irow\"},\n      h(\"span\",{class:\"r-main\"}, h(\"span\",{class:\"r-1\"}, label)),\n      h(\"span\",{class:\"stepper\"}, val,\n        h(\"button\",{\"aria-label\":\"Less: \" + label, onclick:function(){ bump(-1); }},\"−\"),\n        h(\"button\",{\"aria-label\":\"More: \" + label, onclick:function(){ bump(1); }},\"+\")));\n  }\n  // A label wrapping the switch makes the whole 44px row the target, which a\n  // 31px switch on its own is not.\n  function switchRow(label, on, set){\n    var box = h(\"input\",{ type:\"checkbox\", class:\"sw\", \"aria-label\":label });\n    box.checked = !!on;\n    box.addEventListener(\"change\", function(){ set(!!box.checked); });\n    return h(\"label\",{class:\"irow tap\"},\n      h(\"span\",{class:\"r-main\"}, h(\"span\",{class:\"r-1\"}, label)), box);\n  }",
    "risk": "In the same edit delete `var WHO = document.getElementById(\"who\");` at line 174, delete the `renderWho();` call from the top of render(), and delete `<span id=\"who\" hidden></span>` from the static shell. Leaving the call in place with WHO gone would throw at clear(undefined) in the browser while the stub would happily pass, which is exactly the class of bug the harness cannot see."
  },
  {
    "what": "The rating buttons were 26.6 by 25px, the smallest targets in the app and failing on both axes. They become a 44px segmented control, the note and its button get their own rows, and the sentence that describes behaviour the code does not have is replaced.",
    "where": "feedbackCard(), lines 1658 to 1672",
    "before": "      h(\"div\",{class:\"row\"},\n        h(\"button\",{onclick:function(){ saveNote(sk, note.value); }},\"Save note\"),\n        h(\"span\",{class:\"tiny muted\"},\"Two 5s in a row and the next session drops 100 m. Two 1s and it goes up.\")));",
    "after": "      h(\"button\",{class:\"wide\",style:{marginTop:\"12px\"},\n        onclick:function(){ saveNote(sk, note.value); }},\"Save note\"),\n      h(\"div\",{class:\"tiny muted\",style:{marginTop:\"8px\"}},\n        \"The rating is saved with the session so you can see later which ones were too hard.\"));",
    "risk": "The old sentence is false: difficulty_rating is written, displayed and read by nothing, and main_min_m and main_max_m are only ever read from settings. Clear the replacement with Vlad, since the alternative is building adaptive volume, which is a coaching decision and not a design one. Replace the rest of the function in the same edit: the five buttons become `h(\"div\",{class:\"seg\",role:\"group\",\"aria-label\":\"How hard was it\"})` with five children carrying class \"on\" and aria-pressed, and the textarea gets its value set with note.value = sk.note || \"\" plus an aria-label."
  },
  {
    "what": "Login, the not-a-coach screen and the cannot-reach-the-database screen. 44px fields with labels, the raw Supabase string mapped to plain English with the original kept underneath for the setup time cases, and the pre block loses its hard-coded background.",
    "where": "renderLogin() 1482 to 1494, renderNotCoach() line 1502, afterLoadFailed() 2095 to 2101",
    "before": "      h(\"pre\",{class:\"small\",style:{background:\"#f5f8fa\",padding:\"10px\",borderRadius:\"9px\",overflowX:\"auto\"}},",
    "after": "      h(\"pre\",{class:\"small\"},",
    "risk": "The pre rule in the stylesheet already carries the nested background, the radius and overflow-x auto, so it is the one element in the app that scrolls sideways and that is correct for a SQL statement. In the same edit: renderLogin gets setTitle(\"Log in\",\"Log in\"), class \"bordered\" and an aria-label on both fields, Enter bound on the email field as well as the password field, a 56px primary button, and the error mapped with `loginError.indexOf(\"Invalid login credentials\") !== -1 ? \"That email and password do not match. Try again.\" : loginError` with the raw string kept below in a tiny dim line. afterLoadFailed keeps every word and only swaps its two buttons to class \"primary big\" and \"wide\". Note that verify.js renders the not-configured path synchronously, so keep all three free of the banned vocabulary."
  },
  {
    "what": "Export the pool string helpers. They are pure string functions, so verify.js can assert them directly instead of trusting a screenshot, and that is where the 48px type budget actually lives.",
    "where": "window.WSTRAIN_ENGINE, lines 2123 to 2124",
    "before": "    renderCard: renderSessionCard,\n    clearApp: function(){ clear(APP); }",
    "after": "    renderCard: renderSessionCard,\n    clearApp: function(){ clear(APP); },\n    // pure string helpers, so the pool type budget can be asserted rather than\n    // eyeballed\n    poolLines: poolLines, restPhrase: restPhrase, warmupText: warmupText,\n    startPool: startPool, renderPool: renderPool, renderView: render",
    "risk": "Adding exports cannot break the suite: verify checks that eleven names are present, never that others are absent. Tested with these six added. startPool, renderPool and renderView are what let a smoke test paint pool view without a browser."
  },
  {
    "what": "Delete section 15 of the stylesheet, the old class names. Last edit, not before: it is what keeps every screen styled while the edits above land one at a time.",
    "where": "stylesheet section 15, from the header comment down to the last #pool .pblk.done rule",
    "before": "  /* --------------------------------------------------------------------------\n     15. Old class names the script still writes. Delete this block once the\n     last screen is converted, not before, or the app loses its styling halfway\n     through the job.\n     -------------------------------------------------------------------------- */",
    "after": "",
    "risk": "Only safe once no view emits header, nav.tabs, .filters, .hrow, .hdate, .hmain, .htitle, .lrow, .lmain, .pline, .pgoal or .clock.behind. Grep the script for each of those eleven strings before deleting, because a class renamed in CSS only, whose class the script still writes, loses its styling with no test signal at all. Keep .tiles, .tile, .bar and the .blk .line alias: Insights and the ending block still use them."
  }
]